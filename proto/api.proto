syntax = "proto3";

package api;

service ProdService {
    rpc GetProds(ClientRequest) returns (ClientResponse);
}

message ClientRequest {
    string vendor = 1;
}

message ClientResponse {
    string products = 1;
}
